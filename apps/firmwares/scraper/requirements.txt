# GSMArena Ultimate Scraper v6 — Python dependencies

# Core scraping framework
scrapling>=0.4.0

# Output formats
pyarrow>=14.0        # Parquet output (optional — disable with --output-formats csv,sqlite)
openpyxl>=3.1.0      # Excel output (optional)

# Performance (optional — falls back to stdlib asyncio if not installed)
uvloop>=0.19.0; sys_platform != "win32"

# Bloom filter (optional — falls back to plain set dedup)
# bitarray>=2.9.0    # uncomment for memory-efficient bloom filter

# Development / type checking (not required at runtime)
# pyright>=1.1.350
# pytest>=8.0
